models:
  - name: dim_clientes
    description: Dimensão de clientes.
    columns:
      - name: PK_CLIENTE
        description: Chave primária da tabela cliente.
      - name: FK_TERRITORIO
        description: Chave estrangeira da tabela território.
      - name: NOME_CLIENTE
        description: Nome completo do cliente.